import 'package:flutter/material.dart';

class Two_Tap extends StatelessWidget {
  const Two_Tap({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child:Center(
        child: Text("segundo primer tap")
      ),
    );
  }
}